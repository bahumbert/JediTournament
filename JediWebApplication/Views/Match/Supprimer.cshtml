@model JediWebApplication.ServiceReference1.MatchContract

@{
    ViewBag.Title = "Supprimer";
}

<h2>Supprimer</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>MatchContract</h4>
    <hr />
    <input type="text" name="IdJediVainqueur" value="@ViewBag.Matchs.IdJediVainqueur" disabled />
    <input type="text" name="Jedi1" value="@ViewBag.Matchs.Jedi1.Nom" disabled/>
    <input type="text" name="Jedi2" value="@ViewBag.Matchs.Jedi2.Nom" disabled/>
    <input type="text" name="Stade" value="@ViewBag.Matchs.Stade.Nom" disabled/>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
